# FLIPPY Worker requirements
# The worker imports Django directly from the backend — it needs the same
# dependencies as the backend, minus the HTTP-serving stack.

# Django (ORM only — no runserver needed)
Django==5.0.1
psycopg2-binary==2.9.10
dj-database-url==2.1.0
python-dotenv==1.0.0
python-decouple==3.8

# DRF + simple-jwt (imported transitively by backend code)
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1

# Scanner dependencies
playwright>=1.40.0
beautifulsoup4>=4.12.0
geopy>=2.3.0
requests>=2.31.0
httpx>=0.24.0

# AI
google-genai>=0.2.0

# Notifications
sendgrid>=6.10.0
twilio>=8.2.0

# Misc
pytz==2023.3
django-simple-history==3.4.0
django-cors-headers==4.3.1
whitenoise==6.6.0
